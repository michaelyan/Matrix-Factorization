import time
import Matrix_Factorization

#Arguments are num_users, num_items, num_features
#num_users / num_items will determine which users and which items are used in the training set and
#num_features determines how many features the matrix will be factorized to
Matrix_Factorization.test(300, 500, 10)
